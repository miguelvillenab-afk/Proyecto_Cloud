# docker-compose.prod.app.yml — corre IGUAL en app-1 y app-2 (2 MV producción)
# Las 5 APIs. Sin BDs locales. Apuntan a la IP privada de MV-DB vía .env.prod (DB_HOST_PRIVADO).
# Comunicación interna MS3/MS4 por nombres de servicio (api_users, api_properties, api_reservations).
# Uso en cada MV-APP: docker compose -f docker-compose.prod.app.yml --env-file .env.prod up -d --build
services:
  api_users:
    build: ./user_microservice
    ports:
      - "8000:8000"
    env_file:
      - .env.prod
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST_PRIVADO}:5432/${POSTGRES_DB}
    restart: unless-stopped

  api_properties:
    build: ./properties_microservice
    ports:
      - "8081:8081"
    env_file:
      - .env.prod
    environment:
      DB_HOST: ${DB_HOST_PRIVADO}
      DB_PORT: 3306
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      SERVER_PORT: 8081
    restart: unless-stopped

  api_reservations:
    build: ./reservations_microservice
    ports:
      - "3000:3000"
    env_file:
      - .env.prod
    environment:
      PORT: 3000
      MONGODB_URI: mongodb://${DB_HOST_PRIVADO}:27017/reservations_db
      USER_SERVICE_URL: http://api_users:8000
      PROPERTY_SERVICE_URL: http://api_properties:8081
    depends_on:
      - api_users
      - api_properties
    restart: unless-stopped

  api_history:
    build: ./history_microservice
    ports:
      - "8082:8082"
    env_file:
      - .env.prod
    environment:
      PORT: 8082
      USER_SERVICE_URL: http://api_users:8000
      RESERVATIONS_SERVICE_URL: http://api_reservations:3000
      PROPERTY_SERVICE_URL: http://api_properties:8081
    depends_on:
      - api_users
      - api_reservations
      - api_properties
    restart: unless-stopped

  api_analytics:
    build: ./analytics_microservice
    ports:
      - "8003:8003"
    env_file:
      - .env.prod
    environment:
      PORT: 8003
      AWS_REGION: ${AWS_REGION}
      ATHENA_DB: ${ATHENA_DB}
      ATHENA_OUTPUT_S3: ${ATHENA_OUTPUT_S3}
    restart: unless-stopped
